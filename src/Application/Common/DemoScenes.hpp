#pragma once

namespace AppCommon
{
    // Registers the showcase and stress-test ".proc" demo scenes with the loader
    // registry. Call once from the application entry before scene scanning.
    void RegisterDemoScenes();
}
