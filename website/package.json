{
  "name": "gknext-website",
  "version": "1.0.0",
  "description": "Modern lightweight official homepage for gkNextEngine",
  "type": "module",
  "scripts": {
    "dev": "npm run fetch:discussions && vitepress dev",
    "build": "npm run fetch:discussions && vitepress build",
    "preview": "vitepress preview",
    "fetch:discussions": "node scripts/fetch-discussions.mjs",
    "check:discussions": "node scripts/fetch-discussions.mjs --check"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
