# 会话初始化检查清单

## 🚨 **Claude Agent 每次会话开始前必须执行！**

### 第一步：任务类型识别 (5秒)
- [ ] **问自己**：用户的请求是否涉及以下任何一项？
  - 代码开发/修改/重构
  - 构建系统操作
  - 测试/验证
  - 调试/问题排查
  - 新功能开发
  - 性能优化

- [ ] **如果答案是否** → 可以直接回答，无需继续检查清单
- [ ] **如果是** → 必须继续执行以下步骤

### 第二步：立即读取 AGENT_GUIDE (30秒)
- [ ] 读取 `AGENT_GUIDE/README.md` 了解任务类型指导
- [ ] 根据任务复杂度选择阅读层级：
  ```markdown
  | 任务类型 | 推荐阅读层级 | 预估时间 |
  |---------|-------------|---------|
  | 简单任务 | Layer 1 | 30秒 |
  | 复杂开发 | Layer 1+2 | 2.5分钟 |
  | 紧急修复 | Layer 3 | 15秒 |
  ```

### 第三步：创建任务跟踪 (10秒)
- [ ] 立即使用 `TodoWrite` 工具创建任务清单
- [ ] 将 AGENT_GUIDE 中的关键步骤转换为 todo 项目
- [ ] 确保每个 todo 都有明确的 activeForm

### 第四步：严格遵循工作流程
- [ ] 按照选定层级的指导执行任务
- [ ] **Layer 1**: 遵循核心模式 → 构建验证 → 运行验证
- [ ] **Layer 1+2**: 额外遵循上下文规则和代码约定
- [ ] **Layer 3**: 使用快速命令和应急解决方案

### 第五步：成功标准验证
- [ ] 确保 **每次代码修改后** 都进行构建验证
- [ ] 确保 **重要修改后** 进行运行验证
- [ ] 检查是否达到 AGENT_GUIDE 中定义的成功标志：
  - 构建：无错误和警告
  - 运行：看到 `uploaded scene [CornellBox.proc] to gpu`

## ⚡ **快速决策树**

```
用户请求 → 是否涉及代码/构建/测试？
         ↓
    是 → 立即读 AGENT_GUIDE → 选择层级 → TodoWrite → 执行
         ↓
    否 → 直接回答
```

## 🎯 **典型例子**

### ✅ **需要执行检查清单的请求**：
- "帮我实现一个新功能"
- "修复这个编译错误"
- "优化渲染性能"
- "添加支持XXX平台"
- "重构这段代码"

### ❌ **可以直接回答的请求**：
- "这个函数是做什么的？"
- "查看一下这个文件的内容"
- "项目的架构是怎样的？"
- "什么是Vulkan？"

## 📋 **违反检查清单的后果**

如果跳过这个检查清单，你可能会：
- ❌ 遗漏关键的构建验证步骤
- ❌ 不了解项目的特定工作流程
- ❌ 违反代码约定和架构原则
- ❌ 无法提供符合项目标准的解决方案

## 💡 **记忆提示**

**记住这个优先级**：
1. 🥇 AGENT_GUIDE 中的工作流程
2. 🥈 用户的特定请求
3. 🥉 通用的编程最佳实践

**每次会话开始时都要在心里默念：是否涉及代码/构建/测试？**

---
*此检查清单确保每次会话都能遵循项目的最佳实践和工作流程。*