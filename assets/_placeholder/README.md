# 占位资产说明（请勿入库）

这些文件来自作者本地安装的 Brotato 正版游戏解包，仅用于 `Brotato3D` 在个人机器上的临时占位打磨。

这些资产受 Blobfish Games 版权保护，只能用于个人本地构建、调试和观感验证，不能提交到 git，不能打包分发，不能用于任何公开演示。

在公开发布、录屏、截图对外展示或进入任何发行产物之前，这个目录下的全部内容都必须替换为自制、CC0 或已取得授权的资产。

仓库根 `.gitignore` 已明确排除 `assets/_placeholder/`；CI、打包脚本和发布流程也必须继续排除该目录。
