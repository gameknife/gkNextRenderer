---
task: 00024
created: 2026-05-26T00:00:00+08:00
type: FEAT
depends_on: 00023
---

# #00024 AIService Agent 化 — Phase 8：MagicaLego 接入

## 背景

[MagicaLegoAIService](../../src/Application/Game/MagicaLego/MagicaLegoAIService.cpp) 当前同样是 single-shot + 大 prompt 模式。Phase 7 已稳定后，按相同结构迁过来。

## 范围

### In Scope

- `MagicaLegoAIService` 切到 `FAgentLoop`
- 新增 `MagicaLegoTools.{hpp,cpp}`，注册 MagicaLego 专属工具：
  - `lego_list_blocks()` — 当前积木库
  - `lego_place(x, y, z, blockType, rotation?)`
  - `lego_remove(x, y, z)`
  - `lego_select_block_type(name)`
  - `lego_camera_focus(x, y, z)`
  - `lego_save_replay(name)` / `lego_load_replay(name)`（high-risk）
- 复用 #00020 的通用 RepoTools（视情况，可能 game runtime 关掉源码读取，只留 `read_file` for assets/scripts）

### Out of Scope

- 其他 game 的 AI 接入（按需后续做）

## 改动文件

- 改：`src/Application/Game/MagicaLego/MagicaLegoAIService.{hpp,cpp}`
- 新：`src/Application/Game/MagicaLego/MagicaLegoTools.{hpp,cpp}`

## 验收

- MagicaLego 内 AI 对话能通过 agent 流自主放置 / 删除积木
- 老 replay 功能不退化

## 风险

- MagicaLego 主线程 tick 节奏与 EditorContext 不同：`FMainThreadDispatcher` 必须能被多个上下文复用（Engine 单例）
