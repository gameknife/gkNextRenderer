---
id: 00039
title: StudioSim 员工头顶 overlay 层级修复
date: 2026-06-08
status: done
---

## 做了什么

- 将 `DrawWorldOverlay()` 的绘制目标从 `ImGui::GetForegroundDrawList()` 改为 `ImGui::GetBackgroundDrawList()`。
- 员工名牌、对话气泡、POI 标签和浮字现在绘制在 ImGui 面板后方，不再压在 HUD/modal 上面。

## 改动文件

- `src/Application/Game/StudioSim/StudioSimGameInstance.cpp`

## 验证

- `.\gnb.bat build StudioSim`：通过。
- `.\gnb.bat shot --target StudioSim --scene assets/scad/office.scad --frames 180`：通过，日志确认 `uploaded scene [office.scad] to gpu`。
- `git diff --check -- src/Application/Game/StudioSim/StudioSimGameInstance.cpp .spec/TODO.md`：无空白错误；仅提示当前工作区文件会被 Git 触碰时转换 CRLF。

## 风险 / 遗留

- `gnb shot` 截图不包含 ImGui HUD，因此这次视觉层级主要由代码路径确认；交互运行时可最终肉眼确认 overlay 已在面板后方。
