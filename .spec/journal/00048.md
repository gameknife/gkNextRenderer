---
task: 00048
completed: 2026-06-08T23:40:58+08:00
build_ok: true
---

## 做了什么

为 `gnb todo next` 增加 `--wait` 轮询模式：有任务时立即返回；无任务时等待 `.spec/TODO.md` 修改；超时后返回 `found: false`。同步更新交互式工作流规则，让 AGENT 只调用该命令，不再自己拼 mtime/sleep 逻辑。

## 改动文件

- `tools/gnb/cmd/gnb/todo.go`
- `tools/gnb/cmd/gnb/main_test.go`
- `AGENTS.md`
- `.spec/README.md`
- `docs/guides/gnb-cli.md`（现行位置）
- `.spec/TODO.md`
- `.spec/journal/00048.md`

## 风险/遗留

- `go test ./...` 在 Windows 上因 `internal/fetcher` symlink 测试缺少创建符号链接权限失败；本次相关包 `go test ./cmd/gnb ./internal/spec` 已通过。
