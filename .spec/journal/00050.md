---
id: 00050
type: FEAT
date: 2026-06-08
title: Remote Play 质量收尾
---

## 做了什么

- Remote Play 未显式指定 `--remote-res` 时，默认编码目标从源窗口分辨率限制到 1280x720 以内并保持宽高比，避免 OpenH264 软编默认吃 1080p。
- `OpenH264Encoder` 增加 `SetBitrate()`，可在 encoder 已启动后通过 `ENCODER_OPTION_BITRATE` 更新目标码率。
- input DataChannel 增加 `RequestKeyframe` 控制消息；Web 客户端在 input channel 打开后立即请求关键帧，服务端收到后触发 `ForceIntraFrame`。

## 改动文件

- `src/Engine/Runtime/Engine.cpp`
- `src/Engine/Runtime/Remote/OpenH264Encoder.hpp`
- `src/Engine/Runtime/Remote/OpenH264Encoder.cpp`
- `src/Engine/Runtime/Remote/RemoteSession.cpp`
- `assets/remote/index.html`

## 验证

- `.\gnb.bat build gkNextRenderer`
- `.\out\build\windows\bin\gkNextRenderer.exe --remote --agent-validation --agent-validation-frames 5 --hidden-window`
  - 日志显示 `RemotePlay: server started fps=30 bitrate=4000kbps target=1280x720`
  - 日志显示 `uploaded scene [AnimationShowcase.proc] to gpu`
- `.\gnb.bat build gkNextUnitTests`
- `.\out\build\windows\bin\gkNextUnitTests.exe`

## 风险/遗留

- 动态分辨率切换、基于 stats 的自适应码率、Vulkan Video/NVENC 硬编仍未实现，建议继续拆独立任务推进。
