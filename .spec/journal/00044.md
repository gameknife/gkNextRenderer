---
id: 00044
title: WebRTC Remote Play Phase 0
date: 2026-06-08
status: done
---

## 做了什么

- 按 `docs/WebRTC-RemotePlay-Design.md` 先落地 Remote Play Phase 0 基础设施：新增 `--remote`、端口、绑定地址、码率、帧率、分辨率等启动参数；remote 模式默认隐藏窗口并强制 SDR。
- 接入桌面端依赖：`libdatachannel[ws,srtp]`、`openh264`、`cpp-httplib`，并在 CMake 中用 `GK_WITH_REMOTE` 做平台/依赖开关。
- 新增 `Runtime::Remote::RemoteServer` 与 `FSignalingServer`，提供 HTTP `/`、`/config` 和 WebSocket signaling hello；服务生命周期挂到 `NextEngine::Start/Tick/End`。
- 新增 `assets/remote/index.html` 作为浏览器客户端入口，能读取配置、连接 signaling WebSocket，并展示当前 HTTP/signaling/stream 状态。
- 新增 `libjuice` 和 `libdatachannel` vcpkg overlay：修正当前 registry 下 `libjuice` GitHub tarball 哈希漂移问题，并让 `libdatachannel` 走固定 commit 的 `vcpkg_from_git`，避开不稳定的 GitHub archive 下载。

## 改动文件

- `vcpkg.json`
- `cmake/vcpkg-overlays/libjuice/*`
- `cmake/vcpkg-overlays/libdatachannel/*`
- `assets/CMakeLists.txt`
- `assets/remote/index.html`
- `src/CMakeLists.txt`
- `src/Engine/Options.hpp`
- `src/Engine/Options.cpp`
- `src/Engine/Runtime/Engine.hpp`
- `src/Engine/Runtime/Engine.cpp`
- `src/Engine/Runtime/Remote/RemoteServer.*`
- `src/Engine/Runtime/Remote/SignalingServer.*`

## 验证

- `vcpkg.json` / `vcpkg-configuration.json` JSON 解析通过。
- `.\gnb.bat build gkNextRenderer --reconfigure` 通过，CMake 输出 `Remote Play enabled: LibDataChannel::LibDataChannel + httplib::httplib`。
- `.\gnb.bat build gkNextUnitTests` 通过。
- `.\out\build\windows\bin\gkNextUnitTests.exe` 通过：118 个 test case，4145 个 assertion。
- Remote smoke test 通过：隐藏启动 `gkNextRenderer --remote --remote-bind=127.0.0.1 --remote-http-port=18096 --remote-port=18097 --load-scene=assets/models/playground.glb --width=640 --height=360`，`/config` 返回端口/码率/分辨率配置，`/` 返回 Remote Play 页面，WebSocket upgrade 返回 101 并收到 `gkNextRemote` hello；日志包含 `uploaded scene [playground.glb] to gpu`。
- `git diff --check` 通过，仅有既有 CRLF 提示；新增 overlay patch 在临时解压的原始源码上用 `git apply --check --ignore-whitespace` 验证可应用。

## 风险和遗留

- 这是 Phase 0：当前只实现 HTTP 客户端入口和 signaling 服务骨架，尚未实现 H.264 编码、RTP/PeerConnection 媒体发送、DataChannel 输入回传、TURN/鉴权和码率自适应。
- 当前服务启动时会明确记录 LAN-only 未鉴权警告，不能暴露到公网。

## 工作流

- 完成后已写回 `.spec/TODO.md` 并复扫“下一步”，当前无新的可执行任务。
