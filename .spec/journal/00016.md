---
task: 00016
completed: 2026-05-26
build_ok: true
---

## 做了什么

按照 AGENTS.md 当前内容对照 `src/` 实际结构、`tools/gnb/cmd/` 实际命令、最近 commits（dashboard chat、loc tab、GPU CSM）以及 `.spec/journal/` 改动，做了一轮维护：

**修过时**：
- "currently ~15k LOC" → ~85k（实际 `gnb loc` 报 86,373）
- 架构树根目录从 `src/Runtime` / `src/Vulkan` / `src/Rendering` / `src/Assets` 修正到 `src/Engine/{Runtime,Vulkan,Rendering,Assets,Common,NextGameplay,Utilities}`（这些目录是 #00013 周边 refactor 后的真实位置；旧 README 仍写在 `src/` 顶层）
- `src/Application/Game/` 下补全游戏列表：MagicaLego、Brotato3D、KongLie3D、BrickPlayer、CharacterDemo、Flappy(Cpp+Js)、Voyage3D（旧列表只 MagicaLego）
- gnb subprojects 注释格式归位

**补新增**：
- gnb 命令清单加 `loc` / `visual` / `paks` / `dashboard`
- 新增 "gnb Dashboard" 段，介绍 tabs（含 LOC tab = 本里程碑的 #00014）
- 新增 "Rendering / Shadows" 段，介绍三条管线 + GPU CSM 资源结构 + `SampleSunShadowCSM` 位置（#00013/#00015 落地）
- AGENT_GUIDE 文档清单补齐：`QuickJSBindings.md` / `HotReload.md` / `LDrawLoader.md` 三篇之前没列

**精简**：
- 把臃肿的 `Local LLM (gnb llm)` 段（53 行）压成 28 行：相同信息但去掉重复说明，命令合并成单行带 `[…]` 选项，诊断要点保留。
- 总文件从 252 行 → 227 行（缩 10%）。

## 改动文件

- `AGENTS.md` —— 见上

新增：
- `.spec/journal/00016.md`

## 风险/遗留

- AGENTS.md 没有 lint / 文档测试，纯靠 review。若后续模块再 rename，下次再扫一遍。
- `src/Engine/...` 路径下还有更多子目录（如 `Engine/Vulkan/RayTracing/`、`Engine/Assets/Acceleration/`、`Engine/Runtime/Components/` 等）；为了控制篇幅没列到二级目录。如新人觉得太粗可以再加一层，但目前 AGENT_GUIDE/ 已经覆盖细节。
- 旧版本的 "Visual Test Config" 段提到 `assets/configs/visual_test.json`，未验证字段格式是否还匹配 `gkNextVisualTest.cpp`；不在本票范围。

## 验证

- 把 AGENTS.md 重读了一遍，所有路径都在仓库实际存在（`src/Engine/Common/`、`src/Engine/Rendering/Shadow/`、`tools/gnb/internal/dashboard/`、`src/Application/Game/<7 个>` 全部验证 via Glob/Bash）
- `gnb loc` 数字 86k 一致
- gnb 命令列表与 `tools/gnb/cmd/gnb/main.go` 的 `Use:` 字段一致（info / doctor / setup / deps / visual / editor / ios / paks / list / install / loc）
- AGENT_GUIDE 文档 8 篇与 `ls AGENT_GUIDE/` 输出一致
