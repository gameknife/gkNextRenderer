---
id: 00047
type: FEAT
date: 2026-06-08
title: WebRTC Remote Play MVP 收尾
---

## 做了什么

- 补齐 Remote Play 输入链路：服务端创建 unordered/低重传 input DataChannel，解析键盘、鼠标、滚轮和手柄二进制消息。
- 新增 SDL 输入注入：键鼠/滚轮通过 `SDL_PushEvent` 进入原有 `NextEngine::HandleEvent()`；远程相对鼠标使用专用 mouse id 在主线程走 `xrel/yrel`。
- 新增 SDL3 虚拟手柄封装：浏览器 Gamepad API 状态映射到 SDL 虚拟 gamepad，覆盖 `TickGamepadInput()` 的轮询路径。
- Web 客户端补齐输入采集：键盘 scancode 表、Pointer Lock 相对鼠标、鼠标按钮/滚轮、标准 gamepad 轮询。
- `FrameSource` 从测试图样推进到真实画面：复用现有 screenshot 线性图像，从 swapchain 拷回 BGRA 后缩放/转换为 I420，喂给 OpenH264 + WebRTC video track；失败时才回退测试图样。
- Remote 启动验证覆盖 HTTP `/config`、WebSocket request/offer、DataChannel SDP。

## 改动文件

- `src/Engine/Runtime/Remote/RemoteProtocol.hpp`
- `src/Engine/Runtime/Remote/InputRouter.hpp`
- `src/Engine/Runtime/Remote/InputRouter.cpp`
- `src/Engine/Runtime/Remote/VirtualGamepad.hpp`
- `src/Engine/Runtime/Remote/VirtualGamepad.cpp`
- `src/Engine/Runtime/Remote/FrameSource.hpp`
- `src/Engine/Runtime/Remote/FrameSource.cpp`
- `src/Engine/Runtime/Remote/RemoteSession.hpp`
- `src/Engine/Runtime/Remote/RemoteSession.cpp`
- `src/Engine/Runtime/Engine.cpp`
- `assets/remote/index.html`

## 验证

- `.\gnb.bat build gkNextRenderer --reconfigure`
- `.\gnb.bat build gkNextRenderer`
- `.\gnb.bat build gkNextUnitTests`
- `.\out\build\windows\bin\gkNextUnitTests.exe`
- `.\out\build\windows\bin\gkNextRenderer.exe --remote --agent-validation --agent-validation-frames 5 --hidden-window`
- 临时后台 `gkNextRenderer.exe --remote --hidden-window --remote-res 640x360 --remote-fps 10`，验证 `/config` 正常，WS request 返回 H.264 offer，SDP 包含 `webrtc-datachannel`。

## 风险/遗留

- 当前真实帧路径是 CPU BGRA->I420 + OpenH264，能打通 MVP，但还不是设计文档里的 GPU compute / Vulkan Video 最终性能路径。
- 本机 Node 环境缺少 Playwright，未做浏览器自动化解码帧验证；已用 HTTP/WS/SDP 验证信令和 DataChannel。
- Phase 4 Vulkan Video/NVENC、弱网码率自适应、动态分辨率协商仍建议拆后续独立任务。
