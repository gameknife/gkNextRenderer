---
task: 00053
completed: 2026-06-12T20:54:10+08:00
build_ok: false
---

## 做了什么

新增 QuickJS 模块化迁移计划，基于当前代码和 CMake 调用面明确：

- 用核心层 `IScriptRuntime` factory 保留初始化、输入和 Tick 时序。
- 新建可选静态库 `src/Modules/NextQuickJS/`，通过 `Install/Get` 显式装配。
- 把 runtime、bindings、类型转换和 QuickJS 反射桥全部移出 `src/Engine`。
- 移除 AllTargets 的全局 QuickJS 链接，收敛到 FlappyJs、StudioSim、gkNextEditor 和测试 target。
- 迁移 `RegisterJSCallback`、`QuickJSEntry`、默认 `test.js` 行为，并单独处理 Android 全模块并入问题。
- 给出五阶段实施顺序、验证矩阵、风险和完成定义。

## 改动文件

- `docs/QuickJS-Module-Migration-Plan.md`
- `.spec/TODO.md`
- `.spec/journal/00053.md`

## 风险/遗留

- 本任务只产出计划，没有实施 QuickJS 文件移动。
- 未运行 C++ 构建或测试；文档已通过 `git diff --check`。
- 编辑器仍直接使用 QuickJS C API，计划允许首轮迁移保留，后续再封装 registrar。
